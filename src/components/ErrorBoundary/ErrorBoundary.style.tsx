import styled from '@emotion/styled';

export const ErrorBoundaryWrapper = styled.div`
    .error-boundary-msg {
        color: white;
        border: 1px solid red;
        border-radius: 10px;
        padding: 15px;
        background: red;
    }
`;
